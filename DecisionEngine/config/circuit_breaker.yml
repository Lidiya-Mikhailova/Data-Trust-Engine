defaults:
  failure_threshold: 5
  success_threshold: 2
  recovery_timeout_seconds: 30

per_source:
  primary:
    failure_threshold: 5
    success_threshold: 2
    recovery_timeout_seconds: 30
  secondary:
    failure_threshold: 3
    success_threshold: 3
    recovery_timeout_seconds: 15
